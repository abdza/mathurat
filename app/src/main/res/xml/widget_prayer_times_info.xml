<?xml version="1.0" encoding="utf-8"?>
<!--
  updatePeriodMillis is 30 minutes as a safety net only: the widget schedules its own
  alarm at the next prayer time, and the countdown ticks locally via Chronometer.
-->
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    android:initialLayout="@layout/widget_prayer_times"
    android:previewLayout="@layout/widget_prayer_times"
    android:description="@string/widget_prayer_description"
    android:minWidth="250dp"
    android:minHeight="40dp"
    android:targetCellWidth="4"
    android:targetCellHeight="1"
    android:resizeMode="horizontal|vertical"
    android:widgetCategory="home_screen"
    android:updatePeriodMillis="1800000" />
